# Unity prototype workflow

Use this workflow with a Unity 6.3 LTS project:

1. Ask `prototype` to create one scene and input action.
2. Run EditMode and PlayMode tests in batch mode.
3. Ask `qa-test` to review test output and produce a regression checklist.

The generated game belongs outside this repository.
