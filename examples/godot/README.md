# Godot prototype workflow

Use this workflow with a Godot 4.7 project:

1. Ask `prototype` to create one playable scene and input action.
2. Run the project headlessly and capture the log.
3. Ask `qa-test` to review the log and produce a regression checklist.

The generated game belongs outside this repository.
